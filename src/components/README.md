# src/components

Componentes de UI reutilizáveis do painel (botões, cards, colunas do Kanban, formulários).

- Um componente por arquivo, um único objetivo de export.
- Apresentacionais e reutilizáveis; composição de tela fica em `src/pages`.
